<!DOCTYPE html>
<html lang="es">
  <head>
    <title>BarberSaaS - <%= yield(:title) || "Gestión Profesional de Barberías" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
    
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #e74c3c;
            --accent-color: #f39c12;
            --text-light: #7f8c8d;
            --bg-light: #f8f9fa;
        }
        
        body {
            font-family: 'Poppins', sans-serif;
            line-height: 1.6;
            padding-top: 80px; /* Para el navbar fixed */
        }
        
        .navbar-custom {
            background: linear-gradient(135deg, var(--primary-color), #34495e) !important;
            box-shadow: 0 2px 20px rgba(0,0,0,0.1);
            padding: 1rem 0;
        }
        
        .navbar-brand {
            font-size: 1.8rem;
            font-weight: 700;
            color: white !important;
        }
        
        .navbar-nav .nav-link {
            color: rgba(255, 255, 255, 0.9) !important;
            font-weight: 500;
            margin: 0 10px;
            transition: all 0.3s ease;
        }
        
        .navbar-nav .nav-link:hover {
            color: var(--accent-color) !important;
        }
        
        .btn-custom {
            padding: 8px 20px;
            border-radius: 25px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: all 0.3s ease;
            border: none;
        }
        
        .btn-primary-custom {
            background: var(--secondary-color);
            color: white;
        }
        
        .btn-primary-custom:hover {
            background: #c0392b;
            transform: translateY(-1px);
            box-shadow: 0 3px 10px rgba(231, 76, 60, 0.3);
        }
        
        .btn-outline-custom {
            border: 2px solid white;
            color: white !important;
            background: transparent;
            text-decoration: none;
        }
        
        .btn-outline-custom:hover {
            background: white;
            color: var(--primary-color) !important;
            transform: translateY(-1px);
        }
        
        .alert-custom {
            border-radius: 10px;
            border: none;
            padding: 1rem 1.5rem;
            margin-bottom: 1.5rem;
        }
        
        .alert-success {
            background: linear-gradient(135deg, #27ae60, #2ecc71);
            color: white;
            border: none;
        }
        
        .alert-danger {
            background: linear-gradient(135deg, #e74c3c, #c0392b);
            color: white;
            border: none;
        }
        
        .container-custom {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 15px;
        }
        
        /* Dashboard styles */
        .dashboard-header {
            background: linear-gradient(135deg, var(--primary-color), #34495e);
            color: white;
            padding: 2rem 0;
            margin: -1rem -15px 2rem -15px;
            border-radius: 0 0 15px 15px;
        }
        
        .card-modern {
            border: none;
            border-radius: 15px;
            box-shadow: 0 5px 20px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
        }
        
        .card-modern:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 30px rgba(0,0,0,0.12);
        }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark fixed-top navbar-custom">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand" do %>
          <i class="fas fa-cut me-2"></i>BarberSaaS
        <% end %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <% if current_user.super_admin? %>
                <li class="nav-item">
                  <%= link_to "Panel Admin", super_admin_dashboards_path, class: "nav-link" %>
                </li>
              <% elsif current_user.owner? %>
                <li class="nav-item">
                  <%= link_to "Sucursales", barbershop_branches_path(barbershop_id: current_user.barbershop.id), class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Barberos", barbershop_barbers_path(barbershop_id: current_user.barbershop.id), class: "nav-link" %>
                </li>
              <% elsif current_user.barber? %>
                <li class="nav-item">
                  <%= link_to "Mis Citas", barbershop_appointments_path(barbershop_id: current_user.barbershop.id), class: "nav-link" %>
                </li>
              <% end %>

              <li class="nav-item">
                  <%= link_to edit_user_registration_path, class: "nav-link" do %>
                    <i class="fas fa-cog me-2"></i> Perfil
                  <% end %>
              </li>

              <li class="nav-item">
                <%= link_to destroy_user_session_path,
                      class: "nav-link",
                      data: { method: :delete, confirm: "¿Estás seguro que deseas cerrar sesión?" } do %>
                  <i class="fas fa-sign-out-alt me-2"></i> Cerrar Sesión
                <% end %>
              </li>

              <li class="nav-item nav-link">
                  <i class="fas fa-user-circle me-1"></i>
                  <%= current_user.name %>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to new_user_session_path, class: "nav-link" do %>
                  <i class="fas fa-sign-in-alt me-1"></i> Ingresar
                <% end %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Main Content -->
    <main>
      <% unless current_page?(root_path) && !user_signed_in? %>
        <div class="container mt-4">
          <!-- Flash Messages -->
          <% if notice %>
            <div class="alert alert-success alert-custom alert-dismissible fade show">
              <i class="fas fa-check-circle me-2"></i>
              <%= notice %>
              <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
            </div>
          <% end %>
          
          <% if alert %>
            <div class="alert alert-danger alert-custom alert-dismissible fade show">
              <i class="fas fa-exclamation-triangle me-2"></i>
              <%= alert %>
              <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
            </div>
          <% end %>
          <br>
          <% if user_signed_in? %>
            <%= render 'dashboards/header', role: current_user.role %>
          <% end %>
          <%= yield %>
        </div>
      <% else %>
        <%= yield %>
      <% end %>
    </main>

    <!-- Footer (solo en homepage si no hay sesión) -->
    <% unless user_signed_in? %>
      <footer class="bg-dark text-white py-4 mt-5">
        <div class="container">
          <div class="row">
            <div class="col-md-6">
              <p class="mb-0">&copy; <%= Date.current.year %> BarberSaaS. Todos los derechos reservados.</p>
            </div>
          </div>
        </div>
      </footer>
    <% end %>

    <!-- Bootstrap JS (incluye Popper necesario para dropdowns) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
    
    <!-- Custom JS -->
    <script>
      document.addEventListener('DOMContentLoaded', function() {
        // Auto-hide alerts after 5 seconds
        document.querySelectorAll('.alert:not(.alert-permanent)').forEach(function(alert) {
          setTimeout(function() {
            if (typeof bootstrap !== 'undefined') {
              var bsAlert = new bootstrap.Alert(alert);
              bsAlert.close();
            } else {
              alert.style.display = 'none';
            }
          }, 5000);
        });

      });
    </script>
  </body>
</html>
